spring:

  datasource:
    url: jdbc:h2:mem:estoquetest;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: chave-super-segura-utilizada-apenas-nos-testes-automatizados-estoque-api-2026